import React from 'react'

import './Card.scss';

function Card(props) {
    // need the recipe information (title, time, tags, steps, image, userid (for user information))
    // need the user information (username and profile image) - can be fetched if have access to recipe userid
  return (
    <div className='Card'>

    </div>
  )
}

export default Card